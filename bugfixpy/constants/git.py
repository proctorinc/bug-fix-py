import os
# URL for SCW Content Git repository
SCW_CONTENT_URL = "https://github.com/SCWContent/"

# Directory to where the repositories are saved to
REPO_DIR = os.path.join(os.path.dirname(__file__), '../../data/repos')
